import 'package:flutter/material.dart';

class ReceiveTask extends StatelessWidget {
  const ReceiveTask({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Receive task"),
        ),
      ),
    );
  }
}
